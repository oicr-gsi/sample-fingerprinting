To install the webtool you will need to set up apache directory for cgi-bi and html files
Pass this dir as a parameter to installation script during interactive setup:

perl Build.PL
./Build
./Build install

(To reconfigure the base dir run ./Build reconfig)

